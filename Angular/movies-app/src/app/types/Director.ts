// EXPORT - makes something available outside of its own file
export interface Director {
    id?: number;            // ? - makes it so the property is optional
    firstName: string;
    lastName: string;
}